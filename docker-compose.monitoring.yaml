version: '3.8'

# Monitoring stack for Polybot services
# Usage: docker-compose -f docker-compose.monitoring.yaml up -d
# This connects to the polybot-network to scrape metrics from application services

services:
  prometheus:
    image: prom/prometheus:v2.48.0
    container_name: polybot-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - ./monitoring/prometheus/alerts.yml:/etc/prometheus/alerts.yml
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=30d'
      - '--web.enable-lifecycle'
    restart: unless-stopped
    networks:
      - polybot-monitoring
      - polybot_polybot-network  # Connect to app network to scrape metrics

  grafana:
    image: grafana/grafana:10.2.2
    container_name: polybot-grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:-changeme}
      - GF_USERS_ALLOW_SIGN_UP=false
    restart: unless-stopped
    depends_on:
      - prometheus
    networks:
      - polybot-monitoring
      - polybot_polybot-network  # Connect to app network

  alertmanager:
    image: prom/alertmanager:v0.26.0
    container_name: polybot-alertmanager
    ports:
      - "9093:9093"
    volumes:
      - ./monitoring/alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml
      - alertmanager-data:/alertmanager
    environment:
      - SLACK_WEBHOOK_URL=${SLACK_WEBHOOK_URL}
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
      - '--storage.path=/alertmanager'
    restart: unless-stopped
    networks:
      - polybot-monitoring

  node-exporter:
    image: prom/node-exporter:v1.7.0
    container_name: polybot-node-exporter
    ports:
      - "9100:9100"
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    restart: unless-stopped
    networks:
      - polybot-monitoring

volumes:
  prometheus-data:
  grafana-data:
  alertmanager-data:

networks:
  polybot-monitoring:
    driver: bridge
  polybot_polybot-network:
    external: true  # Use the network from docker-compose.yml

